import { CouleurDirective } from './couleur.directive';

describe('CouleurDirective', () => {
  it('should create an instance', () => {
    const directive = new CouleurDirective();
    expect(directive).toBeTruthy();
  });
});
